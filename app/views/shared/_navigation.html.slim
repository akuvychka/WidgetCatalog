.nav-logo
  h1
    | WM
    span v1.0
.nav-links
  ul
    li = link_to 'All Widgets', widgets_path, class: "button nav #{controller_name == 'widgets' ? 'active' : ''}"
    li = link_to 'My Widgets', my_widgets_path, class: "button nav #{controller_name == 'my_widgets' ? 'active' : ''}"
    li = link_to 'Profile', me_users_path, class: "button nav #{controller_name == 'users' ? 'active' : ''}"
    li = link_to 'Log out', sessions_path, method: :delete, data: { confirm: 'Are you sure?' }